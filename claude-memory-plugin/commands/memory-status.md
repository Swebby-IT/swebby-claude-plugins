---
description: "Mostra lo stato corrente della memoria del progetto"
---

# /memory-status — Stato Memoria

Esegui il comando:

```bash
claude-memory status
```

Mostra all'utente il risultato. Se il comando non è trovato, suggerisci di eseguire `/memory-init` prima.
