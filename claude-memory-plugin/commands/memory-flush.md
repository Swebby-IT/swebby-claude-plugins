---
description: "Forza il salvataggio della memoria della sessione corrente"
---

# /memory-flush — Flush Memoria

Esegui il comando:

```bash
claude-memory flush
```

Mostra all'utente il risultato. Se il comando non è trovato, suggerisci `/memory-init`.
