---
description: "Ri-indicizza tutti i file .memory/ in Qdrant"
argument-hint: "[--full] [--sessions-only]"
---

# /memory-reindex — Ri-indicizzazione Qdrant

Esegui il comando:

```bash
claude-memory reindex $ARGUMENTS
```

Mostra all'utente il risultato. Se il comando non è trovato, suggerisci `/memory-init`.
